The Code preforms the following Operations:

circle mycircle;: Creates a circle object named mycircle.
circle* circleptr = &mycircle;: Initializes circleptr to point to mycircle.
circleptr->draw();: Calls the draw() method of the circle class.
rectangle myrectangle;: Creates a rectangle object named myrectangle.
rectangle* rectangleptr = &myrectangle;: Initializes rectangleptr to point to myrectangle.
rectangleptr->draw();: Calls the draw() method of the rectangle class.

Thanks :-D
